# AI写作软件 - APIJSON后端集成

## 🚀 项目介绍

本项目在原有AI写作软件的基础上，成功集成了腾讯开源的APIJSON框架，提供了完整的后端服务支持，实现了用户管理、数据持久化、AI-Writer云端生成等功能。

## ✨ 新增特性

### 🔐 用户管理系统
- **用户注册/登录**: JWT令牌认证，安全的密码加密
- **权限控制**: 管理员、普通用户、访客三级权限
- **个人资料**: 完善的用户信息管理

### 💾 数据持久化
- **项目管理**: 创建、编辑、删除写作项目
- **版本控制**: 文档内容版本管理
- **云端同步**: 多设备数据同步

### 🤖 AI-Writer云端集成
- **云端生成**: AI内容生成记录和统计
- **质量评估**: 自动评估生成内容质量
- **使用反馈**: 用户反馈收集系统

### 📊 数据统计分析
- **写作统计**: 字数、时长、会话等详细统计
- **项目进度**: 实时进度计算和预估
- **热门题材**: 题材分布和使用趋势

### 🔍 APIJSON ORM框架
- **零代码开发**: 自动生成增删改查接口
- **万能通用接口**: 前端可定制任何数据结构
- **自动文档**: 实时生成API文档
- **权限校验**: 自动进行权限验证

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                     前端应用层                               │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐         │
│  │ 用户界面     │ │ AI-Writer   │ │ 写作编辑器   │         │
│  └─────────────┘ └─────────────┘ └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   APIJSON中间层                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐         │
│  │ 认证中间件   │ │ 权限控制    │ │ 自定义函数   │         │
│  └─────────────┘ └─────────────┘ └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层                                │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐         │
│  │ MySQL数据库  │ │ Redis缓存   │ │ 文件存储     │         │
│  └─────────────┘ └─────────────┘ └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

## 📁 项目结构

```
OKComputer_云上写作AI软件/
├── frontend/                          # 前端文件
│   ├── index.html                    # 主页面
│   ├── dashboard.html                # 仪表板
│   ├── templates.html                # 模板库
│   ├── settings.html                 # 设置页面
│   ├── main.js                       # 主要逻辑
│   ├── ai-writer.js                  # AI-Writer模块
│   └── ...                          # 其他前端资源
├── backend/                          # 后端目录
│   ├── package.json                  # 依赖配置
│   ├── index.js                      # 服务器入口
│   ├── config/                       # 配置文件
│   │   ├── database.js              # 数据库配置
│   │   └── apijson-config.js        # APIJSON配置
│   ├── middleware/                   # 中间件
│   │   └── auth.js                  # 认证中间件
│   ├── functions/                    # 自定义函数
│   │   └── custom-functions.js       # 业务函数
│   ├── utils/                        # 工具模块
│   │   └── logger.js                # 日志模块
│   ├── frontend/                     # 前端集成文件
│   │   └── api-client.js            # API客户端
│   └── .env.example                  # 环境变量模板
├── backend-Integration.js             # 前后端集成脚本
├── AI-Writer使用指南.md               # AI-Writer使用指南
├── APIJSON后端部署指南.md             # 部署指南
└── README-APIJSON.md                  # 本文档
```

## 🛠️ 核心功能

### 1. APIJSON ORM框架

APIJSON是一个专为API设计的JSON网络传输协议和ORM库，提供：

- **万能通用接口**: 一个接口处理所有增删改查操作
- **自动权限校验**: 基于角色和表级权限控制
- **实时文档**: 自动生成和维护API文档
- **零代码开发**: 大部分API无需手动编写

**示例API调用：**
```javascript
// 查询用户项目
const projects = await apiClient.get({
  '[]': {
    'document_project': {
      user_id: currentUser.userId,
      status: { '!=': 'archived' }
    },
    'document_content': {
      '@column': 'id,title,updated_at'
    }
  }
});

// 创建新项目
const result = await apiClient.post({
  title: '我的新小说',
  genre: 'fantasy',
  type: 'novel',
  user_id: currentUser.userId
}, 'document_project');
```

### 2. AI-Writer云端集成

AI-Writer功能现在支持云端生成和数据同步：

```javascript
// 云端AI生成
const result = await apiClient.generateAIContent({
  genre: 'fantasy',
  mode: 'continuation',
  prompt: '古老的魔法在空中涌动...',
  projectId: 'project_123'
});

// 生成记录自动保存
// - 生成参数和内容
// - 生成耗时和质量评分
// - 用户反馈和使用统计
```

### 3. 用户权限系统

三级权限控制：

```javascript
// 管理员权限
const adminRole = {
  canAccessAll: true,
  canDeleteAnyUser: true,
  canModifySystemConfig: true,
  canViewAllData: true
};

// 普通用户权限
const userRole = {
  canAccessOwn: true,
  canCreateProject: true,
  canModifyOwnProject: true,
  maxProjects: 100,
  maxDailyWords: 10000,
  maxDailyAIRequests: 50
};

// 访客权限
const guestRole = {
  canViewPublic: true,
  maxProjects: 5,
  maxDailyWords: 1000,
  maxDailyAIRequests: 10
};
```

## 🚀 快速开始

### 1. 后端部署

```bash
# 进入后端目录
cd backend

# 安装依赖
npm install

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，配置数据库等信息

# 启动服务
npm start
```

### 2. 前端配置

```javascript
// 在frontend/api-client.js中配置API地址
const apiClient = new APIClient('http://localhost:3000');

// 或在生产环境中使用环境变量
const apiClient = new APIClient(process.env.API_BASE_URL);
```

### 3. 数据库初始化

启动后端服务时，会自动创建以下数据表：

- `user` - 用户表
- `document_project` - 文档项目表
- `document_content` - 文档内容表
- `ai_writer_log` - AI生成记录表
- `writing_statistics` - 写作统计表
- `system_config` - 系统配置表

### 4. 访问应用

- **前端**: http://localhost:8000
- **后端API**: http://localhost:3000
- **API文档**: http://localhost:3000/v1/doc
- **健康检查**: http://localhost:3000/health

## 📚 API文档

### 认证接口

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/auth/register` | POST | 用户注册 |
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/profile` | GET | 获取用户信息 |

### APIJSON通用接口

| 接口 | 方法 | 描述 |
|------|------|------|
| `/v1/get` | POST | 通用查询接口 |
| `/v1/post` | POST | 通用新增接口 |
| `/v1/put` | PUT | 通用更新接口 |
| `/v1/delete` | DELETE | 通用删除接口 |

### 自定义函数接口

| 函数名 | 参数 | 返回值 | 描述 |
|--------|------|--------|------|
| `getWritingStats` | userId, startDate, endDate | 统计数据 | 获取写作统计 |
| `generateAIContent` | genre, mode, prompt | 生成内容 | AI内容生成 |
| `calculateProjectProgress` | projectId | 进度信息 | 计算项目进度 |
| `getPopularGenres` | userId | 题材统计 | 获取热门题材 |

## 🔧 开发指南

### 1. 添加新的自定义函数

```javascript
// 在backend/functions/custom-functions.js中添加
apijsonServer.addFunction('myFunction', async (request) => {
  try {
    // 业务逻辑
    const result = await businessLogic(request);
    
    return {
      code: 200,
      msg: '操作成功',
      data: result
    };
  } catch (error) {
    return {
      code: 500,
      msg: error.message,
      data: null
    };
  }
});
```

### 2. 添加新的数据表

```javascript
// 在backend/index.js的createTables函数中添加
const newTableSQL = `
  CREATE TABLE IF NOT EXISTS my_table (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    content TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
  ) COMMENT '我的表'
`;

await connection.execute(newTableSQL);
```

### 3. 前端调用新接口

```javascript
// 使用API客户端调用
const result = await apiClient.get({
  'myFunction': {
    parameter1: 'value1',
    parameter2: 'value2',
    '@role': apiClient.user?.role || 'guest'
  }
});

if (result.success && result.data.code === 200) {
  console.log('操作成功:', result.data.data);
}
```

## 🔒 安全特性

### 1. JWT认证
- 使用HMAC SHA256算法签名
- 令牌有效期7天
- 支持令牌刷新

### 2. 密码安全
- 使用bcrypt加密，盐值10轮
- 最小长度6位
- 支持密码强度验证

### 3. API权限控制
- 表级权限控制
- 角色基础访问控制
- 自动SQL注入防护

### 4. 请求限流
- 基于IP的请求限流
- 特殊接口单独限流
- 可配置限流参数

## 📈 性能优化

### 1. 数据库优化
- 索引优化
- 查询优化
- 连接池管理

### 2. 缓存策略
- Redis缓存支持
- 本地内存缓存
- 智能缓存失效

### 3. 前端优化
- API请求合并
- 响应数据缓存
- 懒加载和分页

## 🐛 故障排除

### 常见问题

1. **数据库连接失败**
   - 检查数据库服务状态
   - 验证连接配置
   - 确认网络连通性

2. **JWT认证失败**
   - 检查令牌格式
   - 验证密钥配置
   - 确认令牌有效期

3. **APIJSON请求错误**
   - 检查JSON格式
   - 验证权限设置
   - 查看服务器日志

## 📞 技术支持

- **APIJSON文档**: https://github.com/Tencent/APIJSON
- **项目仓库**: [GitHub链接]
- **问题反馈**: [Issues链接]
- **邮件支持**: support@example.com

## 🎉 致谢

感谢以下开源项目的支持：

- [APIJSON](https://github.com/Tencent/APIJSON) - 腾讯开源的ORM框架
- [Express.js](https://expressjs.com/) - Node.js Web框架
- [MySQL](https://www.mysql.com/) - 关系型数据库
- [Tailwind CSS](https://tailwindcss.com/) - CSS框架

---

## 📄 许可证

本项目采用 MIT 许可证。详见 [LICENSE](LICENSE) 文件。

---

🚀 **让APIJSON为您的AI写作软件提供强大的后端支持！**