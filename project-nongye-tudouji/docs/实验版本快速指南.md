# 农业巡检机器人实验版本快速指南

## 🎯 实验目标

创建一个低成本、快速部署的实验版本，验证核心功能：
- 基础移动能力
- 简单视觉检测
- 远程控制
- 数据采集

---

## 🚀 实验版本配置方案

### 💰 超简化配置（预算：500-800元）

#### 核心硬件清单

| 组件 | 型号 | 价格 | 用途 | 购买渠道 |
|------|------|------|------|----------|
| **主控** | 树莓派 Zero W | ¥150 | 核心控制 | 淘宝 |
| **底座** | 3D打印小车底盘 | ¥80 | 移动平台 | 本地3D打印店 |
| **轮子** | TT马达+轮子套件 | ¥60 | 驱动 | 淘宝 |
| **摄像头** | 树莓派摄像头模块 | ¥100 | 视觉采集 | 淘宝 |
| **电源** | 20000mAh移动电源 | ¥100 | 供电 | 京东 |
| **传感器** | HC-SR04超声波 | ¥20 | 避障 | 淘宝 |
| **舵机** | SG90小舵机 | ¥15 | 摄像头转动 | 淘宝 |
| **杜邦线** | 公母头杜邦线 | ¥20 | 连接 | 淘宝 |
| **面包板** | 830孔面包板 | ¥15 | 原型测试 | 淘宝 |

**总成本：约560元**

---

## 🛒 采购清单 - 一键购物车

### 淘宝购物车清单
```
搜索关键词加入购物车：
1. "树莓派 Zero W 套装" - ¥150
2. "TT马达 小车轮子套件" - ¥60  
3. "树莓派摄像头 V2" - ¥100
4. "HC-SR04超声波模块" - ¥20
5. "SG90舵机 9g" - ¥15
6. "杜邦线 公母头 40根" - ¥20
7. "面包板 830孔" - ¥15
8. "移动电源 20000mAh" - ¥100
```

### 京东购物车清单
```
1. 罗马仕移动电源 20000mAh - ¥100
```

---

## 🏗️ 快速组装步骤

### 第一步：底盘制作（1小时）

#### 方案A：3D打印底盘
```bash
# 下载3D模型文件
wget https://example.com/raspberry-pi-car-chassis.stl

# 本地3D打印店打印
# 文件：chassis.stl
# 材料：PLA
# 耗时：约4小时
# 费用：¥80
```

#### 方案B：手工DIY底盘（0成本）
**材料**：
- 硬纸板/泡沫板（快递包装）
- 热熔胶
- 剪刀/美工刀

**制作步骤**：
1. 裁剪20x15cm底板
2. 制作侧边固定条
3. 开孔安装电机和轮子
4. 制作顶盖放置树莓派

### 第二步：电子组装（2小时）

#### 接线图
```
树莓派 Zero W 接线：
├── GPIO 17 → 左电机正极
├── GPIO 18 → 左电机负极  
├── GPIO 22 → 右电机正极
├── GPIO 23 → 右电机负极
├── GPIO 24 → 超声波 Trig
├── GPIO 25 → 超声波 Echo
├── GPIO 12 → 舵机信号线
├── CSI接口 → 摄像头模块
├── 5V → 舵机电源
└── GND → 公共地线
```

#### 代码验证
```python
# test_motors.py - 电机测试
import RPi.GPIO as GPIO
import time

GPIO.setmode(GPIO.BCM)
GPIO.setup(17, GPIO.OUT)
GPIO.setup(18, GPIO.OUT)

try:
    GPIO.output(17, GPIO.HIGH)
    GPIO.output(18, GPIO.LOW)
    print("前进测试...")
    time.sleep(2)
    GPIO.output(17, GPIO.LOW)
    GPIO.output(18, GPIO.LOW)
    print("停止")
except KeyboardInterrupt:
    GPIO.cleanup()
```

### 第三步：软件部署（1小时）

#### 系统安装
```bash
# 1. 下载Raspberry Pi OS Lite
wget https://downloads.raspberrypi.org/raspios_lite_arm64/images/raspios_lite_arm64-2023-12-06/2023-12-06-raspios-bookworm-arm64-lite.img.xz

# 2. 烧录到SD卡（使用Raspberry Pi Imager）

# 3. 启用SSH和摄像头
# 在boot分区创建ssh文件
touch /boot/ssh

# 4. 启用摄像头
sudo raspi-config
# 选择 Interface Options → Camera → Enable
```

#### 核心软件安装
```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Python依赖
pip install opencv-python picamera flask numpy

# 克隆项目代码
git clone https://github.com/project-nongye-tudouji.git
cd project-nongye-tudouji

# 复制实验版本配置
cp config/experimental/* config/
```

---

## 🧪 核心功能验证

### 功能1：基础移动控制

#### 控制代码
```python
# motor_control.py
import RPi.GPIO as GPIO
from time import sleep

class MotorController:
    def __init__(self):
        GPIO.setmode(GPIO.BCM)
        # 左电机
        GPIO.setup(17, GPIO.OUT)
        GPIO.setup(18, GPIO.OUT)
        # 右电机  
        GPIO.setup(22, GPIO.OUT)
        GPIO.setup(23, GPIO.OUT)
    
    def forward(self, duration=1):
        GPIO.output(17, GPIO.HIGH)
        GPIO.output(18, GPIO.LOW)
        GPIO.output(22, GPIO.HIGH)
        GPIO.output(23, GPIO.LOW)
        sleep(duration)
        self.stop()
    
    def backward(self, duration=1):
        GPIO.output(17, GPIO.LOW)
        GPIO.output(18, GPIO.HIGH)
        GPIO.output(22, GPIO.LOW)
        GPIO.output(23, GPIO.HIGH)
        sleep(duration)
        self.stop()
    
    def left_turn(self, duration=0.5):
        GPIO.output(17, GPIO.LOW)
        GPIO.output(18, GPIO.LOW)
        GPIO.output(22, GPIO.HIGH)
        GPIO.output(23, GPIO.LOW)
        sleep(duration)
        self.stop()
    
    def right_turn(self, duration=0.5):
        GPIO.output(17, GPIO.HIGH)
        GPIO.output(18, GPIO.LOW)
        GPIO.output(22, GPIO.LOW)
        GPIO.output(23, GPIO.LOW)
        sleep(duration)
        self.stop()
    
    def stop(self):
        GPIO.output(17, GPIO.LOW)
        GPIO.output(18, GPIO.LOW)
        GPIO.output(22, GPIO.LOW)
        GPIO.output(23, GPIO.LOW)
    
    def cleanup(self):
        GPIO.cleanup()
```

#### 测试方法
```bash
python3 -c "
from motor_control import MotorController
import time

mc = MotorController()
print('测试前进...')
mc.forward(2)
time.sleep(1)
print('测试左转...')
mc.left_turn(1)
time.sleep(1)
print('测试后退...')
mc.backward(2)
mc.cleanup()
print('测试完成！')
"
```

### 功能2：简单视觉检测

#### 图像处理代码
```python
# simple_detection.py
import cv2
import numpy as np
from picamera import PiCamera
import time

class SimpleDetector:
    def __init__(self):
        self.camera = PiCamera()
        self.camera.resolution = (640, 480)
        
    def capture_image(self):
        # 拍照保存到临时文件
        self.camera.capture('/tmp/current.jpg')
        return cv2.imread('/tmp/current.jpg')
    
    def detect_green_object(self, image):
        # 转换为HSV色彩空间
        hsv = cv2.cvtColor(image, cv2.COLOR_BGR2HSV)
        
        # 绿色范围（植物颜色）
        lower_green = np.array([35, 40, 40])
        upper_green = np.array([85, 255, 255])
        
        # 创建掩码
        mask = cv2.inRange(hsv, lower_green, upper_green)
        
        # 查找轮廓
        contours, _ = cv2.findContours(mask, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
        
        results = []
        for contour in contours:
            area = cv2.contourArea(contour)
            if area > 500:  # 过滤小目标
                x, y, w, h = cv2.boundingRect(contour)
                center_x = x + w // 2
                center_y = y + h // 2
                results.append({
                    'center': (center_x, center_y),
                    'area': area,
                    'bbox': (x, y, w, h)
                })
                
                # 在图像上绘制标记
                cv2.rectangle(image, (x, y), (x+w, y+h), (0, 255, 0), 2)
                cv2.circle(image, (center_x, center_y), 5, (0, 0, 255), -1)
        
        # 保存检测结果
        cv2.imwrite('/tmp/detection_result.jpg', image)
        
        return results

# 测试视觉检测
if __name__ == "__main__":
    detector = SimpleDetector()
    
    print("拍摄图像...")
    image = detector.capture_image()
    
    print("检测绿色物体...")
    results = detector.detect_green_object(image)
    
    print(f"检测到 {len(results)} 个目标:")
    for i, result in enumerate(results):
        print(f"目标{i+1}: 位置{result['center']}, 面积{result['area']}")
    
    print("结果已保存到 /tmp/detection_result.jpg")
```

#### 运行测试
```bash
python3 simple_detection.py
# 查看检测结果
display /tmp/detection_result.jpg
```

### 功能3：Web远程控制

#### Flask控制界面
```python
# web_control.py
from flask import Flask, render_template_string, request, jsonify
import threading
import time
from motor_control import MotorController
from simple_detection import SimpleDetector

app = Flask(__name__)
mc = MotorController()
detector = SimpleDetector()

HTML_TEMPLATE = """
<!DOCTYPE html>
<html>
<head>
    <title>农业巡检机器人控制</title>
    <style>
        body { font-family: Arial; margin: 20px; }
        .control-panel { 
            display: grid; 
            grid-template-columns: 1fr 1fr 1fr 1fr; 
            gap: 10px; 
            max-width: 400px; 
            margin: 20px 0; 
        }
        button { 
            padding: 20px; 
            font-size: 16px; 
            background: #4CAF50; 
            color: white; 
            border: none; 
            border-radius: 5px; 
            cursor: pointer;
        }
        button:hover { background: #45a049; }
        .status { 
            padding: 10px; 
            background: #f0f0f0; 
            border-radius: 5px; 
            margin: 10px 0; 
        }
        .camera-feed { 
            width: 100%; 
            max-width: 640px; 
            border: 1px solid #ccc; 
        }
    </style>
</head>
<body>
    <h1>🤖 农业巡检机器人控制面板</h1>
    
    <div class="status" id="status">
        状态: 等待指令...
    </div>
    
    <div class="control-panel">
        <div></div>
        <button onclick="sendCommand('forward')">↑ 前进</button>
        <div></div>
        <div></div>
        
        <button onclick="sendCommand('left')">← 左转</button>
        <button onclick="sendCommand('stop')">■ 停止</button>
        <button onclick="sendCommand('right')">→ 右转</button>
        <div></div>
        
        <div></div>
        <button onclick="sendCommand('backward')">↓ 后退</button>
        <div></div>
        <div></div>
    </div>
    
    <button onclick="detectObjects()" style="width: 100%; padding: 15px; background: #2196F3;">
        🔍 检测植物
    </button>
    
    <div style="margin-top: 20px;">
        <h3>📷 摄像头画面</h3>
        <img src="/camera_feed" class="camera-feed" id="camera">
    </div>
    
    <script>
        function updateStatus(message) {
            document.getElementById('status').innerHTML = '状态: ' + message;
        }
        
        function sendCommand(cmd) {
            updateStatus('执行: ' + cmd);
            fetch('/control', {
                method: 'POST',
                body: JSON.stringify({command: cmd}),
                headers: {'Content-Type': 'application/json'}
            })
            .then(response => response.json())
            .then(data => updateStatus(data.status));
        }
        
        function detectObjects() {
            updateStatus('正在检测...');
            fetch('/detect')
            .then(response => response.json())
            .then(data => {
                if (data.results.length > 0) {
                    updateStatus(`检测到 ${data.results.length} 个植物`);
                } else {
                    updateStatus('未检测到植物');
                }
                // 刷新摄像头画面
                document.getElementById('camera').src = '/camera_feed?' + new Date().getTime();
            });
        }
        
        // 自动刷新摄像头画面
        setInterval(() => {
            document.getElementById('camera').src = '/camera_feed?' + new Date().getTime();
        }, 5000);
    </script>
</body>
</html>
"""

@app.route('/')
def index():
    return render_template_string(HTML_TEMPLATE)

@app.route('/control', methods=['POST'])
def control():
    data = request.json
    command = data['command']
    
    try:
        if command == 'forward':
            mc.forward(1)
            return jsonify({'status': '前进完成'})
        elif command == 'backward':
            mc.backward(1)
            return jsonify({'status': '后退完成'})
        elif command == 'left':
            mc.left_turn(0.5)
            return jsonify({'status': '左转完成'})
        elif command == 'right':
            mc.right_turn(0.5)
            return jsonify({'status': '右转完成'})
        elif command == 'stop':
            mc.stop()
            return jsonify({'status': '停止'})
    except Exception as e:
        return jsonify({'status': f'错误: {str(e)}'})
    
    return jsonify({'status': '未知命令'})

@app.route('/detect')
def detect():
    try:
        image = detector.capture_image()
        results = detector.detect_green_object(image)
        return jsonify({'results': results})
    except Exception as e:
        return jsonify({'error': str(e)})

@app.route('/camera_feed')
def camera_feed():
    try:
        detector.camera.capture('/tmp/camera_feed.jpg', use_video_port=True)
        return app.send_static_file('/tmp/camera_feed.jpg')
    except:
        return "摄像头不可用"

if __name__ == "__main__":
    try:
        app.run(host='0.0.0.0', port=5000, debug=False)
    finally:
        mc.cleanup()
```

#### 启动Web控制
```bash
# 启动Web服务
python3 web_control.py

# 在手机或电脑上访问
# http://树莓派IP地址:5000
```

---

## 🧪 实验场景测试

### 场景1：室内植物巡检

**测试环境**：
- 放置3-5盆绿植在室内
- 设置障碍物模拟田间环境
- 使用手机Web界面控制

**测试目标**：
- [ ] 机器人能够前进后退
- [ ] 能够识别绿色植物
- [ ] 能够避开障碍物
- [ ] Web界面控制流畅

**测试代码**：
```python
# auto_patrol.py
from motor_control import MotorController
from simple_detection import SimpleDetector
import time

mc = MotorController()
detector = SimpleDetector()

def auto_patrol():
    print("开始自动巡检...")
    
    for i in range(10):  # 巡检10次
        print(f"第{i+1}次巡检")
        
        # 前进2秒
        mc.forward(2)
        
        # 停止拍照检测
        time.sleep(0.5)
        image = detector.capture_image()
        results = detector.detect_green_object(image)
        
        if results:
            print(f"发现 {len(results)} 个植物目标")
            for result in results:
                print(f"位置: {result['center']}")
        
        # 右转90度
        mc.right_turn(0.8)
        
        time.sleep(1)
    
    print("巡检完成")
    mc.cleanup()

if __name__ == "__main__":
    auto_patrol()
```

### 场景2：基础避障测试

**避障代码**：
```python
# obstacle_avoid.py
import RPi.GPIO as GPIO
import time
from motor_control import MotorController

class ObstacleAvoidance:
    def __init__(self):
        GPIO.setmode(GPIO.BCM)
        GPIO.setup(24, GPIO.OUT)  # Trig
        GPIO.setup(25, GPIO.IN)   # Echo
        self.mc = MotorController()
    
    def get_distance(self):
        # 发送超声波脉冲
        GPIO.output(24, GPIO.HIGH)
        time.sleep(0.00001)
        GPIO.output(24, GPIO.LOW)
        
        # 测量回波时间
        start_time = time.time()
        while GPIO.input(25) == 0:
            start_time = time.time()
        
        while GPIO.input(25) == 1:
            end_time = time.time()
        
        # 计算距离
        distance = (end_time - start_time) * 17150
        return round(distance, 2)
    
    def auto_avoid(self):
        print("启动避障模式...")
        
        try:
            while True:
                distance = self.get_distance()
                print(f"前方距离: {distance}cm")
                
                if distance < 20:  # 小于20cm时避障
                    print("检测到障碍物，执行避障")
                    self.mc.backward(1)
                    self.mc.right_turn(1)
                else:
                    self.mc.forward(0.5)
                
                time.sleep(0.1)
                
        except KeyboardInterrupt:
            print("停止避障")
            self.mc.stop()
            GPIO.cleanup()

if __name__ == "__main__":
    oa = ObstacleAvoidance()
    oa.auto_avoid()
```

---

## 🚀 部署和测试时间表

### 📅 第一天：硬件组装（4小时）

**上午（2小时）**：
- [ ] 购买材料送达
- [ ] 组装底盘
- [ ] 安装电机和轮子

**下午（2小时）**：
- [ ] 连接电子元件
- [ ] 焊接杜邦线
- [ ] 基础通电测试

### 📅 第二天：软件部署（3小时）

**上午（2小时）**：
- [ ] 烧录系统
- [ ] 安装依赖库
- [ ] 配置摄像头

**下午（1小时）**：
- [ ] 部署控制代码
- [ ] 基础功能测试

### 📅 第三天：功能验证（2小时）

**上午（1小时）**：
- [ ] 电机控制测试
- [ ] 视觉检测测试

**下午（1小时）**：
- [ ] Web控制测试
- [ ] 综合功能测试

---

## 🎯 验证标准

### ✅ 基础功能清单

| 功能 | 验证方法 | 成功标准 |
|------|----------|----------|
| 移动控制 | Web界面按键 | 机器人按指令前进、后退、转向 |
| 视觉检测 | 拍照检测植物 | 能够识别80%以上的绿色植物 |
| 远程控制 | 手机/电脑访问 | Web界面响应时间<2秒 |
| 避障功能 | 放置障碍物 | 距离<20cm时自动避让 |
| 数据采集 | 巡检记录 | 保存检测结果和位置信息 |

### 📊 性能指标

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| 响应时间 | <2秒 | Web指令到动作完成 |
| 检测精度 | >80% | 10次测试的成功率 |
| 续航时间 | >2小时 | 连续运行时间 |
| 控制距离 | >10米 | WiFi有效范围 |

---

## 🔧 常见问题解决

### Q1: 树莓派无法启动
**解决方案**：
1. 检查SD卡是否正确烧录
2. 确认电源输出≥5V/2A
3. 检查HDMI线缆连接

### Q2: 电机不转动
**解决方案**：
1. 检查GPIO接线是否正确
2. 确认电机电源充足
3. 测试电机驱动代码

### Q3: 摄像头无法工作
**解决方案**：
1. 在raspi-config中启用摄像头
2. 检查排线是否松动
3. 重启系统再测试

### Q4: WiFi连接问题
**解决方案**：
1. 检查WiFi名称和密码
2. 确认信号强度足够
3. 重启网络服务

---

## 📈 下一步升级计划

### 🚀 升级路径1：增强感知（+300元）
- 添加超声波传感器阵列
- 增加红外传感器
- 集成温湿度传感器

### 🚀 升级路径2：智能控制（+500元）
- 升级树莓派4B
- 添加激光雷达模块
- 集成IMU姿态传感器

### 🚀 升级路径3：执行机构（+800元）
- 添加微型水泵
- 安装简易机械臂
- 集成LED照明系统

---

## 🎉 实验成功标准

如果您的实验版本能够完成以下任务，说明基础实验成功：

✅ **基础移动**：能够通过Web界面控制前进后退转向  
✅ **视觉检测**：能够识别出环境中的绿色植物  
✅ **远程控制**：能够通过手机或电脑远程控制  
✅ **数据记录**：能够保存检测到的目标信息  

一旦基础版本验证成功，就可以开始逐步升级到完整版本！

---

## 📞 技术支持

如果在实验过程中遇到问题，可以通过以下方式获得帮助：

- **项目文档**：查看完整的技术文档
- **GitHub Issues**：提交问题到项目仓库
- **技术论坛**：树莓派和机器人开发社区
- **在线教程**：YouTube上大量相关教程

---

**文档版本：V1.0**  
**更新日期：2024年1月**  
**适用范围：快速原型验证**  
**预计成功率：95%**